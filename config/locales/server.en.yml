en:
  site_settings:
    skip_moderation_enabled: "Enable skipping moderation by certain groups?"
    skip_moderation_groups_categories: "List of groupslug:categoryslug where the group avoids moderation for that category"
